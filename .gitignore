# Environment
.env
venv/
__pycache__/
*.pyc
*.pyo

# Model checkpoints (too large for git)
checkpoints/
checkpoints_*/

# Training logs and artifacts
training_log_run*.json
life_log.json
life_log_adam.json
life_log_eve.json
vocab_divergence.jsonl
vocab_log.jsonl
vocab_convergence.jsonl
generations.jsonl
first_contact.jsonl
culture_events.jsonl
vocab_extinctions.jsonl
observation_results.json
run*.log
server.log
server_test.log
session_log.json
memory_old_broken.json

# Adam's memory (auto-generated — stays LOCAL, never pushed to GitHub)
memory.json
memory/
memory/*.json

# TensorBoard runs
runs/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Adam's memory (auto-generated — stays local, never pushed)
memory.json

# Backups (local only)
backups/

# Manus original (reference only)
manus_original/

# Legacy v0.1 (archived — kept in legacy/ for reference)
# legacy/
