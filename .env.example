# Nafs AI Environment Variables
# Currently unused — PPO+GRU brain runs fully offline with no API keys needed.
# This file exists for future local LLM integration (v1.0).

# Path to the local LLM GGUF model file (future use)
NAFS_MODEL_PATH=models/stablelm-zephyr-3b.Q4_K_M.gguf
